package bricker.package_factory;

/**
 * Enum representing the different types of
 * special strategies available in the game.
 * Each enum value corresponds to a specific strategy.
 * The enum values are used to create instances
 * of the corresponding strategies.
 * The enum values are used to create instances
 * @author tomer gottman, michael messika
 */
public enum specialStrategiesOptions {
    /**
     * Add balls strategy.
     */
    ADD_BALLS,
    /**
     * Camera strategy.
     */
    CAMERA,
    /**
     * Double paddle strategy.
     */
    DOUBLE_PADDLE,
    /**
     * Falling heart strategy.
     */
    FALLING_HEART,
    /**
     * Increase score strategy.
     */
    DOUBLE
}